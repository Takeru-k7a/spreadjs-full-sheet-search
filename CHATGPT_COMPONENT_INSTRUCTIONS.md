# ChatGPT向け 実装指示書

この指示書は、SpreadJS検索ダイアログと同じ考え方で、既存アプリに差し込みやすいReact/TypeScriptパーツをChatGPTに作らせるためのものです。

## 依頼文テンプレート

以下の内容をChatGPTにそのまま渡してください。

```text
あなたはReact/TypeScriptの実装担当です。
既存アプリに組み込みやすい、独立したUIパーツを1フォルダで作成してください。

目的:
- 既存の画面やホストライブラリに強く依存しない、再利用可能なコンポーネントにする
- 親アプリ側はコンポーネントを1つ配置し、必要なインスタンスやデータ取得関数をpropsで渡すだけにする
- UI、状態管理、実処理ロジック、公開API、デモ、READMEをひとまとまりで提供する

必須技術:
- React
- TypeScript
- MUI Material
- 既存アプリ側のCSSに依存しない
- Next.jsやViteなどのクライアント実行環境で使えること

出力してほしいファイル:
- index.ts
- MainComponent.tsx
- componentStore.ts
- componentEngine.ts
- componentUtils.ts
- demo/MainComponentDemo.tsx
- README.md
- package.json

実装方針:
- MainComponent.tsxはUIとユーザー操作だけを担当する
- componentEngine.tsは検索、集計、変換などの実処理だけを担当する
- componentStore.tsは開閉状態、位置、入力値、結果などの共有状態を管理する
- componentUtils.tsは正規化、フォーマット、座標変換などの純粋関数を置く
- index.tsから利用者に必要なコンポーネント、props型、公開API、主要なユーティリティをexportする
- ファイル冒頭に、そのファイルが何の目的で存在するかを短いコメントで書く
- 複雑な処理には、なぜその処理が必要なのかが分かるコメントを添える
- 外部アプリ固有のimport、グローバル変数、DOM構造への依存を避ける
- 型は明示し、anyは原則使わない

UI要件:
- UIはMUI MaterialのBox、Paper、Stack、Button、IconButton、TextField、Checkbox、Tableなどで作る
- 通常のCSSファイルやCSS Modulesに寄せすぎず、MUIのsxやコンポーネント設計を中心にする
- ダイアログ型パーツの場合は、固定表示かつドラッグ移動できるようにする
- ドラッグはpointerdown後にwindowのpointermove/pointerupで追跡し、マウスがヘッダー外へ出ても動くようにする
- 初期表示位置はpropsまたはstoreの初期値で変更できるようにする
- 位置を初期値へ戻すAPIまたはUIを用意する
- 閉じるUIを用意し、外部APIからも閉じられるようにする
- キーボードショートカットが自然なパーツでは、Ctrl+F / Cmd+Fなどをサポートする
- 入力欄、チェックボックス、結果一覧、実行ボタン、閉じるボタンなどの状態が分かりやすいこと

状態管理:
- コンポーネント外からも開閉や位置変更ができるように、軽量なstoreを作る
- React 18以降ではuseSyncExternalStoreを使って外部storeを購読する
- close、open、toggle、setPosition、resetPositionなどの関数をexportする
- localStorageなどの永続化は、依頼がない限り入れない

ホスト連携:
- ホストライブラリのインスタンスはコンポーネント内部で生成しない
- propsでgetHost、getWorkbook、getDataSourceなどの関数を受け取る
- 実行タイミングで最新のインスタンスやデータを取得する
- 画面初期化後にデータ読み込みされるケースを考慮し、初期render時だけでなく検索・実行ボタン押下時にも最新状態を見る
- 処理対象が未初期化の場合は落とさず、分かりやすい空状態またはエラーメッセージにする

検索・抽出系パーツの場合:
- 実行時に最新データを走査する
- 可能なら利用中範囲を優先し、取得できない場合はフォールバック範囲を走査する
- 表示値を取得できるAPIがある場合は、内部値ではなく表示値を優先する
- 完全一致、部分一致、大文字小文字無視、全角半角正規化などのオプションを用意する
- 結果はスナップショットとして保持し、クリックで対象位置へ移動できるようにする
- 大量データ向けにmaxResultsや走査上限を用意する
- debugオプションを用意し、対象範囲、ヒット数、未初期化などをconsole.logで確認できるようにする

READMEに書くこと:
- このパーツの目的
- インストールが必要なpeer dependencies
- 最小利用例
- props一覧
- 外部公開API一覧
- 初期表示位置の変更方法
- キーボードショートカット
- デバッグ方法
- デモの使い方
- このパーツが責務外にしていること

検証:
- TypeScriptの型チェックが通ること
- buildが通ること
- デモ画面で初期表示、開く、閉じる、移動、位置リセット、キーボードショートカット、主要機能が動くこと
- debug=trueで必要な調査ログが出ること
- READMEの最小利用例が実装と一致していること

最終回答:
- 追加したファイル一覧
- 使い方の要点
- 検証結果
- 既知の制約
を短くまとめてください。
```

## 設計の狙い

この指示の狙いは、UIパーツを「既存画面の一部」ではなく「外から差し込める小さな製品」として作らせることです。親アプリに密結合すると再利用や検証が難しくなるため、ホスト連携はpropsと公開APIに寄せます。

## ファイル分割の考え方

- `MainComponent.tsx`: 画面表示、入力、クリック、ショートカット、ドラッグなどのUI操作を扱う。
- `componentStore.ts`: 開閉状態、ウィンドウ位置、入力値、検索結果など、UIをまたいで共有したい状態を扱う。
- `componentEngine.ts`: ホストデータの走査、検索、集計、変換など、UIから切り離せる実処理を扱う。
- `componentUtils.ts`: 文字正規化、座標表記、表示フォーマットなど、テストしやすい純粋関数を扱う。
- `index.ts`: 利用者がimportする入口。公開してよいものだけを集める。
- `demo/`: 実際に動作確認できる最小サンプルを置く。
- `README.md`: 導入者がコードを読まずに使えるように、目的、導入、API、制約を書く。

## 実装時に特に守らせること

- 初期化直後の状態だけを見ない。ユーザー操作のタイミングで最新データを取得する。
- ドラッグ移動はヘッダー内だけで完結させず、`window`のpointerイベントで追跡する。
- UIはMUI Materialを使い、通常CSSへの依存を増やさない。
- 外部から開閉できるAPIをexportする。
- どのファイルが何のためのものか、コードコメントで分かるようにする。
- 調査が必要な機能には`debug`オプションを用意する。
- READMEのサンプルと実装のexport名を必ず一致させる。

